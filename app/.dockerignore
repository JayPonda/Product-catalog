# Secrets — never enter the image (backend URL is injected via build arg)
.env
.env.*
!.env.example

# Dependencies and build output (rebuilt inside the image)
node_modules/
dist/

# Local dev
coverage/
*.log

# Docker files themselves
Dockerfile
.dockerignore

# Git
.git
.gitignore
