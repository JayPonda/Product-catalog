import { useNotificationStore } from './notifications'

/**
 * @deprecated Use useNotificationStore from '@/stores/notifications' instead.
 * Kept for backward compatibility.
 */
export const useErrorStore = useNotificationStore
